/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "uploadthing.com",
            "utfs.io",
            "tailwindui.com",
            "cdn.discordapp.com",
            "picsum.photos"
        ]
    }
};

export default nextConfig;
